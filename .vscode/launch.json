{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "在 msedge 中啟動並附加",
            "type": "msedge",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/src",
            "sourceMapPathOverrides": {
                "webpack:///src/*": "${workspaceFolder}/src/*"
            }
        }
    ]
}